import React from 'react'

import styled from 'styled-components'

const TextWrapper = styled.h5`
  color: blue;
`

const Banner = () => {
  return <TextWrapper>Banner</TextWrapper>
}

export default Banner
